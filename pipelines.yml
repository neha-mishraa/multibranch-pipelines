template: true
valuesFilePath: ./values.yml

resources:
  - name: mb1
    type: GitRepo
    configuration:
      path: bharath92/multibranch-pipelines
      gitProvider: github
      branches:
        include: ^{{gitBranch}}$

pipelines:
  - name: mbcheck1
    steps:
      - name: basic
        type: Bash
        configuration:
          runtime:
            type: host
          {{ if contains "{{gitBranch}}" "main1" }}
          inputResources:
            - name: mb1
          {{ end }}
        execution:
          onExecute:
            - echo hi
