template: true
valuesFilePath: ./values.yml

resources:
  {{ range $i, $e := until 1500 }}
  - name: monitor_cron_trigger_{{$i}}
    type: CronTrigger
    configuration:
      interval: '0 12 * * *' # every 12 noon
  {{ end }}

pipelines:
  - name: testcron_
    steps:
      - name: basic
        type: Bash
        configuration:
          runtime:
            type: host
          inputResources:
          {{ range $i, $e := until 1500 }}
            - name: monitor_cron_trigger_{{$i}}
          {{ end }}
        execution:
          onExecute:
            - echo hi
