template: true
valuesFilePath: ./values.yml

resources:
  {{ range $i, $e := until 1500 }}
  - name: monitor_cron_trigger{{$i}}
    type: CronTrigger
    configuration:
      interval: '*/5 * * * *' # Every 5 minutes
  {{ end }}

pipelines:
  - name: testcron
    steps:
      - name: basic
        type: Bash
        configuration:
          runtime:
            type: host
          inputResources:
          {{ range $i, $e := until 1500 }}
            - name: monitor_cron_trigger{{$i}}
          {{ end }}
        execution:
          onExecute:
            - echo hi
